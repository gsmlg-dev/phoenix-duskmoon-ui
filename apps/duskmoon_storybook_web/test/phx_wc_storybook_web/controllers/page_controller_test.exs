defmodule DuskmoonStorybookWeb.PageControllerTest do
  use DuskmoonStorybookWeb.ConnCase

  # test "GET /", %{conn: conn} do
  #   conn = get(conn, "/")
  #   assert html_response(conn, 200) =~ "Duskmoon UI"
  # end
end
